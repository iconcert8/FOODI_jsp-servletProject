<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.foodi.mapper.SearchMapper">

<!-- 	<select id="searchUser" parameterType="String" resultType="UserInfoVO">
		select * from userinfo where userNick LIKE ${keyword}
	</select>
	
	<select id="searchFeed" parameterType="String" resultType="FeedVO">
		SELECT f.feedno,f.userid,f.feedcontent,f.feedImg,f.feedImgs,f.feedgoodcnt,f.feedloc,f.feeddate,f.feedupdate,f.feedlock
FROM feed f, feedtag t where f.feedno=t.feedno AND (f.feedcontent LIKE ${keyword} OR t.tagname LIKE ${keyword})
	</select> -->
	
	<insert id="insertSearch" parameterType="SearchVO">
		INSERT INTO search VALUES(search_seq.nextval,#{searchKeyword},sysdate,'a')
	</insert>
</mapper>